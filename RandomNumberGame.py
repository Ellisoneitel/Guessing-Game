import random 
rand = int(random.randrange(0,10))
guess = int(input("Guess the number: "))
    while rand != guess
        if rand > guess
            print "Guess Higher"
        else:
            print "Guess Lower"
        